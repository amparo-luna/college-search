<!DOCTYPE html>
<html>
  <head>
    <title>CollegeMap</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= tag :meta, name: "mapApiKey", content: Rails.application.credentials.google_maps.api_key %>

    <%# <style>
      .div1 {
          width: 20%;
          float: left;
      }
      .div2 {
          width: 80%;
          float: left;
          height: 600px;
          position: relative;
          overflow: hidden;
      }
      .list-group {
          list-style: none;
          padding: 0;
          margin: 0;
      }
      .list-group a {
          display: block;
          padding: 10px 15px;
          margin-bottom: -1px;
          background-color: #fff;
          border: 1px solid #ddd;
          text-decoration: none;
          color: #333;
      }
      .list-group a:hover {
          background-color: #f5f5f5;
      }
    </style> %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
